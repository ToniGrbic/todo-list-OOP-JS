# Todo app

Vanila JS + NodeJS todo list app

# Setup

1. Create a new postgres database in pgAdmin
2. In .env put conn string in `DATABASE_URL`
3. In root run: `npm i` & `npm run dev`
4. App is now accessible on localhost port 3000
